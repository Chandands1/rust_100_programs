fn main() {
    // Define a string
    let my_string = String::from("Hello, Rust!");

    // Get the length using the `len()` method
    let length = my_string.len();

    // Print the result
    println!("The length of the string is: {}", length);
}